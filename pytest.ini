[pytest]
testpaths = tests
addopts = -ra --strict-markers
markers =
    integration: uses the compiled MuJoCo robot model
    rendering: creates an off-screen OpenGL renderer
    slow: exercises an expensive build or model operation
